package org.b333vv.metric.model.util;

public enum CogntiveComplexityElementType {
    WHILE,
    FOR,
    IF,
    ELSE,
    SWITCH,
    CATCH,
    BREAK,
    CONTINUE,
    AND,
    OR,
    RECURSION,
    METHOD,
    UNKNOWN
}
